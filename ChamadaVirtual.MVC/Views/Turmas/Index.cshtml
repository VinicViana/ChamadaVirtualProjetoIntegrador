@model IEnumerable<ChamadaVirtual.MVC.Models.Turma>

@{
    ViewData["Title"] = "Turmas";
}

<h1>Turmas</h1>
<p>
    <a class="btn btn-success" asp-action="Create">Nova turma</a>
</p>
        @foreach (var item in Model)
        {
            <!--<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td>
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Editar</a>-->
                    @*<a asp-action="Details" asp-route-id="@item.Id">Details</a>*@
                    <!--<a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
                    <a class="btn btn-secondary" asp-action="Visualizar" asp-route-id="@item.Id">Visualizar</a>
                </td>
            </tr>-->

            <div class="card m-5">
                <div class="card-header list-group-item-primary"  >
                    @Html.DisplayFor(modelItem => item.Descricao)
                </div>
                <div class="card-body">
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
                    <a class="btn btn-secondary" asp-action="Visualizar" asp-route-id="@item.Id">Visualizar</a>
                </div>
            </div>
        }